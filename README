omgbio PCR multi temperature probe reader
for Dallas Semiconductor DS18x2x family probes.

This is just some early code that we're using to gather              
temperature ramp data as we develop prototype PCR machines.

License for Arduino code: GPLv3
License for Web app: AGPLv3

= Installation =

Prerequisites for the server side:

  sudo aptitude install build-essential libsqlite3-dev

  npm install

Grant access to the user running the server side js:

  sudo chown <user> <serial_port_device>

E.g:

  sudo chown joe /dev/ttyACM0






